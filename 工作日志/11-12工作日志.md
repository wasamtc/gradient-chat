镜像问题解决：

```shell
export HF_ENDPOINT="https://hf-mirror.com"
or
mkdir -p ~/.cache/huggingface/hub
echo '{"endpoint": "https://hf-mirror.com"}' > ~/.cache/huggingface/hub/config.json
```

1.配置container的gpu组件

2.下载最新的parallax并安装，mac上我是删了重新clone，Linux上我是本地clone然后cp到容器内

3.Linux的容器内我使用pip install，应该不会和之前的冲突吧，但是速度很慢

4.解决了pip报错的问题，应该还是镜像，换成清华源

```shell
pip install -e '.[gpu]' -i https://pypi.tuna.tsinghua.edu.cn/simple/
```

5.加上`param-hosting-ration`参数没有看到分开处理不同layer的情况，应该是参数太小了

6.修改参数，提交pr

7.看lora的原理